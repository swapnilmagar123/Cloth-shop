<nav class="fixed w-full top-0 z-50 bg-white/80 backdrop-blur-md shadow-lg">
  <div class="container mx-auto px-6 py-3 flex items-center justify-between">

    <!-- Logo -->
    <a href="/" class="text-2xl font-bold text-indigo-600 flex items-center gap-2 hover:scale-105 transition-transform">
      <i class="bi bi-shop"></i> Cloth Shop
    </a>

    <!-- Hamburger for Mobile -->
    <button id="menu-btn" class="md:hidden text-3xl text-gray-700 focus:outline-none hover:text-indigo-600 transition">
      <i class="bi bi-list"></i>
    </button>

    <!-- Desktop Links -->
    <ul class="hidden md:flex space-x-8 font-medium text-gray-700">

      <li><a href="/" class="hover:text-indigo-600 hover:underline underline-offset-4 transition">Home</a></li>







      
      <li><a href="/products" class="hover:text-indigo-600 hover:underline underline-offset-4 transition">Products</a>
      </li>
      <% if (user) { %>
        <li><a href="/products/add" class="hover:text-indigo-600 hover:underline underline-offset-4 transition">Add
            Product</a></li>
        <% } %>
          <li><a href="/about" class="hover:text-indigo-600 hover:underline underline-offset-4 transition">About</a>
          </li>
          <li><a href="/contact" class="hover:text-indigo-600 hover:underline underline-offset-4 transition">Contact</a>
          </li>
    </ul>

    <!-- Right Side Buttons (Desktop) -->
    <div class="hidden md:flex items-center space-x-4">
      <% if (!user) { %>
        <a href="/login"
          class="px-5 py-2 border border-indigo-600 text-indigo-600 rounded-full hover:bg-indigo-600 hover:text-white transition font-semibold shadow-sm">
          <i class="bi bi-person"></i> Login
        </a>
        <a href="/signup"
          class="px-5 py-2 border border-green-600 text-green-600 rounded-full hover:bg-green-600 hover:text-white transition font-semibold shadow-sm">
          <i class="bi bi-pencil-square"></i> Signup
        </a>
        <% } else { %>
          <span class="text-gray-700 font-medium flex items-center gap-2">
            <% if (user.avatar) { %>
              <img src="<%= user.avatar %>" alt="avatar" class="w-9 h-9 rounded-full border shadow" />
              <% } else { %>
                <i class="bi bi-person-circle text-2xl"></i>
                <% } %>
                  <%= user.username %>
          </span>
          <a href="/logout"
            class="px-5 py-2 border border-red-600 text-red-600 rounded-full hover:bg-red-600 hover:text-white transition font-semibold flex items-center gap-1 shadow-sm">
            <i class="bi bi-box-arrow-right"></i> Logout
          </a>
          <% } %>

            <a href="/cart"
              class="px-5 py-2 bg-gradient-to-r from-pink-500 to-red-500 text-white rounded-full hover:opacity-90 transition font-semibold flex items-center gap-2 shadow-md">
              🛒 Cart 
            </a>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div id="mobile-menu" class="hidden md:hidden bg-white/95 backdrop-blur-md shadow-lg animate-slide-down">
    <ul class="flex flex-col space-y-4 px-6 py-6 font-medium text-gray-700">
      <li><a href="/" class="hover:text-indigo-600 transition">🏠 Home</a></li>
      <li><a href="/products" class="hover:text-indigo-600 transition">🛍 Products</a></li>
      <% if (user) { %>
        <li><a href="/products/add" class="hover:text-indigo-600 transition">➕ Add Product</a></li>
        <% } %>
          <li><a href="/about" class="hover:text-indigo-600 transition">ℹ️ About</a></li>
          <li><a href="/contact" class="hover:text-indigo-600 transition">📩 Contact</a></li>

          <% if (!user) { %>
            <li><a href="/login" class="text-indigo-600 hover:text-indigo-800 transition"><i class="bi bi-person"></i>
                Login</a></li>
            <li><a href="/signup" class="text-green-600 hover:text-green-800 transition"><i
                  class="bi bi-pencil-square"></i> Signup</a></li>
            <% } else { %>
              <li class="flex items-center gap-2">
                <% if (user.avatar) { %>
                  <img src="<%= user.avatar %>" alt="avatar" class="w-8 h-8 rounded-full border shadow" />
                  <% } else { %>
                    <i class="bi bi-person-circle text-xl"></i>
                    <% } %>
                      <%= user.username %>
              </li>
              <li><a href="/logout" class="text-red-600 hover:text-red-800 transition"><i
                    class="bi bi-box-arrow-right"></i> Logout</a></li>
              <% } %>
                <li><a href="/cart" class="text-red-600 font-semibold hover:text-red-800 transition">🛒 Cart (<%=
                      cartCount %>)</a></li>
    </ul>
  </div>
</nav>

<!-- Animation CSS -->
<style>
  @keyframes slide-down {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-slide-down {
    animation: slide-down 0.3s ease-out;
  }
</style>

<script>
  // Toggle mobile menu
  const menuBtn = document.getElementById("menu-btn");
  const mobileMenu = document.getElementById("mobile-menu");

  menuBtn.addEventListener("click", () => {
    mobileMenu.classList.toggle("hidden");
  });
</script>